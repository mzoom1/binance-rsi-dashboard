# Public Binance REST host
NEXT_PUBLIC_BINANCE_BASE=https://api.binance.com

# RSI calculation source: local|binance (local preferred)
RSI_SOURCE=local

# Optional (not required for public endpoints)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Use local seed/mock data (for dev without internet)
MOCK=false
