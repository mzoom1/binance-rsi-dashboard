export const revalidate = 0;

export default function Page() {
  return (
    <div>
      <h1 style={{ margin: 0 }}>Ping / (server)</h1>
      <p style={{ opacity: 0.7 }}>Якщо ви бачите це — базовий SSR працює ок.</p>
    </div>
  );
}
